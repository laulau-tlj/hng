import Navbar from '../../component/navbar'

const Home = ({user}) => {


  console.log(user)
    return (
        <div>
          <Navbar />

            Home
        </div>
    );
};

export default Home;